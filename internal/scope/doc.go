// Package scope parses and evaluates scope.yaml (ranges, ports,
// protocols, binds, dial blacklists, canary). When the scope file is
// absent, the AUP acknowledgement flow applies to every active command.
package scope
