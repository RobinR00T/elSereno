---
phase: v2.14
status: closed
last-updated: 2026-05-15
token-budget: 60
---

# Snapshot — v2.14.0: dashboard clones view

Single-chunk cycle. Closes v2.10 carryover. v2.10 added
`/{id}/clones`; v2.14 adds the dashboard UI.

## Dashboard surface

  - New "Clones" button per schedule row (between
    Sparkline + Clone-action).
  - Opens an inline panel below the schedules table.
  - Table columns: Name / Created / State / Tags / Action.
  - Each row's "Edit" button beginEditSchedule(clone.id)
    + closes the clones view → operator drills into a
    clone in one click.
  - Empty state: "no clones of this schedule".
  - 404 surfaces as "schedule not found".

## JS API (window-exposed)

  - `openClonesView(id, name)`
  - `closeClonesView()`

## Honest scope

  - No recursive chain traversal (single-hop only).
  - No bulk select / delete on the clones table.
  - INSTALL.md unchanged. Build sizes unchanged.

## Build sizes

Unchanged across all 3 variants.

Adds ~80 LoC (HTML + JS) in dashboard.go.

## Carryover

  - **vNext**: `?depth=N` on /clones to walk chains.
  - **vNext**: bulk select in the clones panel.
