---
phase: v1.35
status: closed
last-updated: 2026-05-04
token-budget: 280
---

# Snapshot — v1.35.0: proxy listen --plugin pcworx|mms|enip|s7 + --record

Single-chunk cycle. Closes the v1.30 carryover that flagged
"recording for pcworx + mms + enip + s7 via CLI" as deferred
(those 4 plugins had Recorder fields but no CLI verb).

## Why a tiny cycle

The infrastructure work for capture had already shipped:
- v1.28 chunk 3 added `Recorder *replay.Recorder` to pcworx
  + mms (session-level POC).
- v1.30 chunk 1 added the same field to enip + s7 (wire-
  aware, plus 7 others — sip/iax2/pbxhttp/modbus/opcua/
  bacnet/cwmp).
- v1.30 chunk 2 added the operator-facing `--record FILE`
  flag on `proxy listen`.

But `proxy listen --plugin` only accepted 7 values; pcworx +
mms + enip + s7 were library-only. v1.35 closes that gap.

## Chunks

### Chunk 1 — Wire 4 new plugins (`23aa50c`)

`cmd/elsereno/cmd_proxy_offensive.go` gains:

  - `buildPcworxHandler` / `buildMMSHandler` (session-level;
    take a list of operator-supplied free-text intents that
    roll into the session-mutation hash)
  - `buildENIPHandler` (wire-aware; takes a list of CIP
    encapsulation command codes, uint16)
  - `buildS7Handler` (wire-aware; takes a list of S7
    function codes, uint8 → s7wire.FunctionCode)
  - 4 cases in `buildGatedHandler` dispatcher
  - `registerProxyListenLegacyICSFlags` helper extracted
    from `registerProxyListenFlags` to keep the latter
    under the funlen ceiling
  - 3 new fields on `proxyListenOpts`: intents (string list,
    shared by pcworx + mms), cipCommands (uint list),
    s7Functions (uint list)
  - 4 new arms in `attachRecorder` type-switch — the
    Recorder field already existed on each handler, this
    just makes it reachable via --plugin

`cmd/elsereno/cmd_proxy_allowfile_offensive.go`:
  - 4 new `pluginName*` constants

New CLI flags (all plugin-scoped):

| Flag             | Plugin(s)        | Type    | Purpose |
|------------------|------------------|---------|---------|
| `--intent`       | pcworx, mms      | strings | session-level free-text tags |
| `--cip-command`  | enip             | uints   | CIP encapsulation cmd allowlist |
| `--s7-fc`        | s7               | uints   | S7 function code allowlist |

Range checks:
  - `--cip-command N`: rejected if N > 0xFFFF (CIP cmd is
    16-bit).
  - `--s7-fc N`: rejected if N > 0xFF (S7 FC is 8-bit).

## Test coverage

`cmd/elsereno/cmd_proxy_attachrecorder_offensive_test.go` (NEW):

  - **TestAttachRecorder_AllSupportedHandlers** — table-
    driven, 11 sub-tests covering every handler type that
    ships a Recorder field (sip, iax2, pbxhttp, modbus,
    opcua, bacnet, cwmp, pcworx, mms, enip, s7). A new
    plugin that ships a Recorder field but doesn't get
    added to the type-switch will fail this test —
    mechanical enforcement of "if you add a Recorder
    field, you must add an attachRecorder arm".
  - **TestAttachRecorder_UnsupportedTypeReturnsFalse** —
    pins the default-branch behaviour via a `fakeHandler`
    stub.

Smoke tests verified end-to-end:
  - `--plugin pcworx --intent ...` → reaches accept-writes
    validation (plugin name dispatched, --intent flag
    accepted).
  - `--plugin enip --cip-command 99999` → reaches
    accept-writes (validation defers to the auth fences;
    overflow check fires later when the handler is built).
  - `--plugin pcworx --record FILE ...` → recorder threads
    through the standard --record path; vault failure
    confirms the codepath reached past plugin selection.

## Test totals

  - Chunk 1: +13 tests (11 happy + 2 default-branch).

All pass under -race; lint clean across all 3 build
variants. Existing tests for the pcworx/mms/enip/s7
gated-proxy packages unchanged.

## Build sizes

Unchanged from v1.34 (text-only feature wiring; no new
linked dependencies — pcworx/mms/enip/s7 packages were
already imported transitively via the offensive variant).

  default      23.0 MB
  offensive    23.7 MB
  mini         21.3 MB

## Carryover into v1.36+

  - **Validation of ProConOS fingerprint against real PLC**
    (v1.28 chunk 1; confidence ~0.7).
  - **Validation of GE-SRTP service-0x21 `fw=` field**.
  - **Dashboard `--input` parity** with `scan` + `tui`.
  - **TUI golden-file output assertion**.
  - **Per-(unit, fc, address-range) allowlists for the new
    plugins**: enip + s7 currently take a flat command/FC
    list; richer per-target scoping (mirroring modbus's
    `--write unit=N;fc=M;...` form) is a v1.36+ candidate
    if operator demand surfaces.
