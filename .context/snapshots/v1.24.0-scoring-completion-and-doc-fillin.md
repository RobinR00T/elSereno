---
phase: v1.24
status: in-flight
last-updated: 2026-04-29
token-budget: 700
---

# Snapshot — v1.24.0: scoring completion + doc fill-in

Two chunks completing the v1.23 refinements: extends
CVE-exposure to the remaining ICS + PBX plugins (16 of 25
plugins now publish a non-zero cve_exposure score, was 9 after
v1.23) and fills in the 5 missing engineering notes in
`.context/protocols/` so all 25 plugins now have notes.

No new plugins; no new tests beyond v1.24 chunk 1's modified
buildFinding paths (still fully covered by existing plugin
test suites). Default-build plugin count unchanged at **25**.

## Chunk 1 — CVE-exposure for 7 more plugins

`(commit 4ee8017)` Closes the cve_exposure non-zero coverage
gap on the remaining ICS + PBX plugins.

| Plugin | New cve_exposure | Anchor CVEs |
|---|---:|---|
| s7 | 14 | CVE-2014-2249, CVE-2016-4785, CVE-2018-13815, Stuxnet-era CVE-2010-2772 |
| fox | 13 | CVE-2012-3024 (Niagara hardcoded creds), CVE-2015-2916, CVE-2017-16744 |
| sip | 12 | Asterisk CVE-2009-1207 + family, Cisco CVE-2017-3881, FreeSWITCH CVE-2021-33611, 3CX CVE-2023-29059 |
| enip | 11 | CVE-2017-7898, CVE-2018-19009, CVE-2020-12029, CVE-2021-22681 (all Rockwell) |
| pbxhttp | 11 | FreePBX RCE family CVE-2014-7235 + 2019-19006 + 2020-25822 + 2017-9358 + 2024-41713 |
| iax2 | 9 | CVE-2007-3764, CVE-2008-3263, CVE-2009-3727, CVE-2014-9374 (all Asterisk) |
| modbus | 9 | CVE-2015-1015, CVE-2017-9853, CVE-2018-7240, CVE-2021-22779 (all Schneider) |

After v1.24 chunk 1: **16 of 25 plugins** publish a non-zero
cve_exposure score. Remaining 9 plugins: finsudp / slmp /
gesrtp / knxip / mbustcp / dlms (v1.20-v1.21 fingerprints
where public CVE history is too sparse to score confidently),
atmodem / xot (niche legacy serial protocols, no notable
public CVEs), banner (meta plugin).

## Chunk 2 — Engineering notes for the 5 missing plugins

`(commit 3e64b03)` Adds engineering-level notes in
`.context/protocols/` for the 5 plugins that were missing
them: cwmp, opcua, sip, iax2, pbxhttp. Each follows the same
shape as the existing 20: TL;DR + spec references + wire
format + fingerprint strategy + read ops + write/dial ops
(with chunk-history citations) + REPL plan + proxy hooks +
scoring contribution + sentinel cases.

After v1.24 chunk 2: **all 25 plugins** have engineering
notes in `.context/protocols/`. Combined with the 25
operator-facing summaries in `docs/protocols/`, the protocol
documentation is now complete across both audiences.

## Aggregate stats

| Metric | Value |
|---|---|
| Commits | 2 |
| New protocol plugins | 0 (refinement cycle) |
| Touched plugins | 7 (CVE-exposure values) |
| New engineering notes | 5 (cwmp, opcua, sip, iax2, pbxhttp) |
| New tests | 0 (existing tests cover the modified paths) |
| Default-build plugin count | 25 (unchanged) |
| Plugins with non-zero cve_exposure | 9 → 16 |
| Plugins with engineering notes | 20 → 25 |
| Lint | 0 issues |
| Sec | exit 0 |
| context-check | ok |

## Operator surface at end of v1.24

- 16 of 25 plugins publish a non-zero `cve_exposure` score.
  The dashboard's risk-ranked finding list now reflects the
  CVE landscape across the full ICS + PBX coverage matrix.
- All 25 plugins have both operator-facing summaries
  (`docs/protocols/<name>.md`) and engineering notes
  (`.context/protocols/<name>.md`).

## Deferred to v1.25+

Same backlog as after v1.23:
- macOS sandbox via `sandbox_init(3)`.
- 4 remaining legacy ICS protocols (PROFINET DCP / GOOSE / SV
  Layer-2 only, IEC 61850 MMS port-102 share with S7 + complex
  ASN.1, OPC UA HTTPS).
- Offensive plugins for the 8 fingerprint families shipped
  v1.20-v1.22 (FINS / SLMP / SRTP / KNX / M-Bus / DLMS /
  CoDeSys / RLN write services).
- GE-SRTP service-0x21 follow-up probe.
- TUI (bubbletea), Windows support, OIDC + roles,
  record-&-replay proxy sessions.
